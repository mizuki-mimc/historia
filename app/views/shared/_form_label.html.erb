<div class="flex items-center">
  <%= f.label attribute, text, class: "block text-lg font-bold" %>

  <% if required_attribute?(f.object, attribute) %>
    <span class="ml-2 px-2 py-0.5 rounded-full text-xs font-semibold bg-blue-100 text-blue-800">必須</span>
  <% end %>
</div>
